# Google 드라이브 연결 가이드

강의 도구의 **☁ Google 드라이브** 기능을 쓰면, 개인 PC에서 올린 강의 파일과 진도를 학교 PC에서 구글 로그인만으로 이어서 쓸 수 있습니다.

- 처음 한 번은 Google Cloud 설정(약 15분, 무료, 코딩 없음)이 필요합니다 → **3장**
- 설정이 끝나면 발급된 **클라이언트 ID** 한 줄만 알려 주세요. 앱에 넣어 배포하면 기능이 켜집니다.

---

## 1. 학교(공용) PC 체크리스트 — 매번 이 순서로

1. **시크릿 창**(Chrome `Ctrl+Shift+N`)에서 앱 주소를 엽니다: `https://lecture-assistant-chi.vercel.app`
   - 창을 닫으면 Google 로그인까지 함께 사라져 가장 안전합니다.
2. 첫 화면의 파란 **☁ Google 드라이브 연결** → 내 Gmail로 로그인.
   - Chrome이 **"비밀번호를 저장할까요?" / "Chrome에 로그인할까요?"** 라고 물으면 **안함/거절**.
3. 오른쪽 **구글 드라이브 저장** 목록의 강의를 한 번 클릭 → 받는 중(%) → 저장한 페이지로 열립니다(위치를 저장하지 않은 강의는 1페이지).
4. 강의 중 **📌 위치 저장**을 누르면 진도가 드라이브에 저장됩니다(학교 PC에는 남지 않습니다).
5. 끝나면 **✕ 닫기 → 연결 해제 → Google 로그아웃↗**(연결 해제 옆 링크, 새 창) 순서로 누릅니다.
   - **✕ 닫기를 누르면 "위치 저장하고 닫기 / 저장 없이 닫기 / 취소"를 먼저 묻습니다.** 진도를 남기려면 **위치 저장하고 닫기**(이름은 묻지 않습니다), 그냥 닫으려면 **저장 없이 닫기**를 누르세요.
   - 이 창은 주석이 사라진다는 것과 드라이브에 아직 반영하지 않은 위치가 있다는 것도 함께 알려 줍니다.
   - **연결 해제만으로는 Google 로그인이 브라우저에 남습니다.** 다음 사람이 Gmail·드라이브를 열 수 있으니 로그아웃까지 꼭 하세요.
   - 시크릿 창이었다면 마지막에 **창을 모두 닫으세요.**

> 첫 사용은 **강의 전날이나 강의 10분 전에 리허설**하세요. 학교망이 Google 드라이브를 막는 경우가 있으니, 처음 몇 번은 **USB에도 PDF를 챙기세요.**

---

## 2. ☁ 드라이브 저장과 📌 위치 저장은 역할이 다릅니다

| 버튼 | 하는 일 | 다른 PC에서 보이는 곳 |
|---|---|---|
| **☁ 드라이브 저장** | 지금 연 **강의 파일을 드라이브로 복사**하고 **현재 페이지까지 함께 저장**합니다. 이름은 묻지 않습니다(전에 📌로 붙인 이름, 없으면 파일 이름). | **구글 드라이브 저장** 목록(페이지까지) |
| **📌 위치 저장** | 지금 **페이지를 이름 붙여 저장**합니다(이름을 묻습니다). 드라이브에 올린 강의라면 드라이브에도 함께 저장됩니다. **드라이브의 파일은 바꾸지 않습니다.** | **저장된 강의 (이 PC)**, 드라이브에 올린 강의면 **구글 드라이브 저장**도 함께 |

- 개인 PC에서 강의 준비: 파일 열기 → 시작할 페이지로 이동 → **☁ 드라이브 저장**(파일 복사 + 그 페이지 저장). 이름을 따로 붙이고 싶을 때만 **📌 위치 저장**.
- ☁를 누르지 않은 강의는 드라이브에 아무것도 올라가지 않습니다(자동 업로드 없음).
- **📌는 드라이브 파일을 바꾸지 않습니다.** 드라이브 파일 내용은 ☁에서 직접 **덮어쓰기**를 고른 경우에만 바뀝니다.
- **PDF를 고쳤다면 ☁를 누르세요.** 드라이브에 같은 이름의 다른 파일이 있으면 확인 창이 뜹니다.
  - **덮어쓰기** — 드라이브 파일을 이 PC의 파일로 바꿉니다. 이전 버전은 드라이브의 '버전 관리'에서 일정 기간 복원할 수 있습니다.
  - **다른 이름으로 저장** — `강의자료 (2).pdf`처럼 새 이름으로 올리고 기존 파일은 그대로 둡니다. 목록에는 **별도 강의**로 표시됩니다.
  - **취소** — 아무것도 올리지 않습니다.
- 수정본을 열면 "드라이브에 같은 이름의 다른 버전이 있습니다" 안내가 뜹니다. 이 상태에서 📌는 이 PC에만 저장합니다(토스트 끝에 "(드라이브에는 저장하지 않음)").
- 학교 PC에서 USB의 **옛 버전**을 같은 이름으로 열어도 📌는 드라이브 파일을 바꾸지 않습니다. ☁를 누르면 확인 창이 뜨니 **덮어쓰기를 누르지 마세요.** 학교에서는 USB 대신 드라이브 목록에서 여는 것이 가장 안전합니다.

### 첫 화면의 두 목록

| 목록 | 무엇이 보이나 |
|---|---|
| **저장된 강의 (이 PC)** | 지금 이 PC에서 열거나 저장한 강의(위치를 저장한 것과 최근에 연 것 모두). 시크릿 창을 닫으면 사라집니다. |
| **구글 드라이브 저장** | 드라이브에 올린 강의 전부. 연결하지 않으면 안내 문구만 보입니다. |

- **같은 강의가 양쪽에 각각 한 줄로 보일 수 있습니다**(이 PC에도 있고 드라이브에도 올린 경우). 왼쪽은 이 PC의 파일을 바로 열고, 오른쪽은 드라이브에서 받아서 엽니다.
- 왼쪽 제목 줄의 작은 선택창으로 **날짜순(기본) / 이름순**을 고를 수 있고, **두 목록에 함께 적용**됩니다. 고른 값은 다음에 열 때도 유지됩니다.
- 줄 오른쪽 **✕** — 왼쪽 목록은 이 PC 목록에서만, 오른쪽 목록은 드라이브 목록에서만 지웁니다(확인 창이 뜨고, **PDF 파일은 드라이브에 남습니다**).
- 위치(페이지)를 저장한 강의는 `12페이지 · 오늘 09:30`처럼 보이고, 저장하지 않은 강의는 시각만 보이며 열면 **1페이지**부터 시작합니다.

#### ⬆ 업데이트 — 드라이브 파일을 이 PC의 최신 파일로 바꾸기

**구글 드라이브 저장** 목록의 줄에서 ✕ 왼쪽에 있는 작은 **⬆** 버튼입니다. 같은 강의가 이 PC에도 있을 때만 나타납니다.

- **파란색으로 강조되면** 이 PC 파일이 드라이브 파일과 내용이 다르고 더 최신입니다. 누르면 드라이브 파일을 이 PC 파일로 바꿉니다(새 파일을 만들지 않고 같은 파일을 갱신하며, **저장한 이름과 페이지는 그대로**입니다). 버튼에 마우스를 올리면 `이 PC 2.5MB(9/18) / 드라이브 2.3MB(9/12)`처럼 양쪽 정보가 보입니다.
- **흐린 보통 상태**면 아직 비교하지 못한 것입니다(파일을 열기 전에는 브라우저가 이 PC 파일을 읽을 권한을 주지 않습니다). 그래도 눌러 보세요 — 권한을 물어본 뒤 비교합니다.
- **내용이 같으면 아무것도 올리지 않고** `이미 최신입니다`라고만 알려 주고 버튼이 사라집니다.
- 이 PC 파일이 드라이브 파일보다 **오래된** 경우에는 `그래도 드라이브를 이 PC 파일로 바꿀까요?` 확인 창이 먼저 뜹니다(실수로 옛 버전을 올리지 않도록).
- 버튼이 보이지 않는다면 ①이 PC 목록에 같은 강의가 없거나 ②내용이 이미 같은 경우입니다. 파일 자체를 새로 올리려면 강의를 열고 **☁ 드라이브 저장**을 쓰세요.

---

## 3. 처음 한 번: Google Cloud 설정

### 준비물
- 강의용 **개인 Gmail 계정**(개인 PC·학교 PC에서 같은 계정)
  - 학교·교육청에서 준 Workspace 계정은 관리자가 외부 앱을 막는 경우가 많습니다. **개인 Gmail**을 쓰세요.
- **휴대폰** — 공용 PC 로그인 때 2단계 인증을 요구할 수 있습니다.
- PC 웹브라우저(Chrome 권장)

> 아래 링크는 화면 상단에 **내 프로젝트 이름이 선택되어 있는지** 확인하고 누르세요. 메뉴 이름이 바뀌어도 링크로 바로 갈 수 있습니다.

### 3-1. 프로젝트 만들기
1. **https://console.cloud.google.com** 접속 → 강의용 Gmail로 로그인 → (처음이면) 약관 동의.
2. 왼쪽 위 **프로젝트 선택** → **새 프로젝트** → 이름 `Lecture Annotation Tool` → **만들기**.
3. 알림(종 모양)에서 **프로젝트 선택** → 왼쪽 위에 프로젝트 이름이 보이면 성공.

### 3-2. Google Drive API 켜기
1. **https://console.cloud.google.com/apis/library/drive.googleapis.com** 열기
2. 파란 **사용** 버튼 클릭.
   - 빠뜨리면 앱에서 "드라이브 API가 허용되지 않았습니다"가 뜹니다.

### 3-3. 로그인 안내 화면(Google 인증 플랫폼)
1. **https://console.cloud.google.com/auth/overview** → **시작하기**
2. 단계별 입력:
   - **앱 정보**: 앱 이름 `Lecture Annotation Tool`, 사용자 지원 이메일 = 내 Gmail
   - **대상**: **외부(External)**
   - **연락처 정보**: 내 Gmail
   - **완료**: 정책 동의 체크 → **만들기**
3. **테스트 사용자 추가**: **https://console.cloud.google.com/auth/audience** → **테스트 사용자 → + Add users** → 내 Gmail → 저장
4. (선택) **데이터 액세스**: **https://console.cloud.google.com/auth/scopes** → 범위 추가 → `.../auth/drive.file`
   ("이 앱에서 사용하는 특정 Google Drive 파일만…") 체크 → 저장
   - 이 앱은 **자기가 만든 파일만** 볼 수 있습니다. 내 드라이브의 다른 문서는 볼 수 없습니다.

### 3-4. 클라이언트 ID 만들기
1. **https://console.cloud.google.com/auth/clients** → **클라이언트 만들기**
2. **애플리케이션 유형**: **웹 애플리케이션**, 이름: `강의 도구`
3. **승인된 JavaScript 원본** → **+ URI 추가**로 아래를 **정확히** 입력(끝에 `/` 없이):
   ```
   https://lecture-assistant-chi.vercel.app
   http://localhost:3000
   ```
   - 두 번째 줄은 **내 PC에서 직접 실행할 때만** 필요합니다(없어도 배포 주소는 동작).
4. **승인된 리디렉션 URI는 비워 둡니다** → **만들기**
5. 표시된 **클라이언트 ID**(`…apps.googleusercontent.com`)를 복사해 알려 주세요.
   - 함께 보이는 **클라이언트 보안 비밀번호(secret)는 필요 없습니다.** 누구에게도 보내지 마세요.
   - 클라이언트 ID는 공개돼도 괜찮은 값입니다(등록한 주소에서만 동작).
   - 설정 반영에 몇 분(길게는 몇 시간) 걸릴 수 있습니다.

### 3-5. 앱 게시(권장) — 매주 다시 동의하지 않게
- **테스트** 상태에서는 테스트 사용자의 승인이 **7일마다 만료**되어, 매주 강의 때 동의 화면과 경고가 다시 뜹니다.
- 이 앱이 쓰는 `drive.file`은 **비민감 범위**라서, 게시해도 **Google 심사 대상이 아닙니다**(로고를 올리지 않으면 브랜드 심사도 없음).
- 권장 순서:
  1. 테스트 상태에서 개인 PC ☁ 저장 → 학교 PC 열기를 한 번 확인합니다.
  2. **https://console.cloud.google.com/auth/audience** → **앱 게시(Publish app)** → 확인.
- 게시하면 테스트 사용자 100명 제한도 사라집니다. 로그인 화면에 앱 이름 대신 주소가 보일 수 있습니다(정상).
- 6개월 동안 쓰지 않은 클라이언트는 Google이 자동 삭제합니다(30일 전 메일 안내, 삭제 후 30일 안에 복원 가능). 긴 방학 뒤 로그인이 안 되면 확인하세요.

---

## 4. 처음 로그인할 때 보이는 화면
1. **☁ Google 드라이브 연결** → Google 로그인 팝업.
   - 안 뜨면 주소창 오른쪽 **팝업 차단 아이콘 → 항상 허용** 후 다시 클릭.
2. 계정 선택(공용 PC는 **다른 계정 사용**) → 비밀번호 → (요구 시) 휴대폰 인증.
3. 경고 화면은 두 가지 중 하나입니다.
   - **"Google에서 확인하지 않은 앱"(테스트 상태)** → **계속**
   - 그 밖의 경고 → **고급(Advanced)** → **Lecture Annotation Tool(으)로 이동(안전하지 않음)**
   - 직접 만든 앱이라 뜨는 정상 안내입니다. 3-3의 테스트 사용자에 없는 계정이면 **"403 access_denied"**가 뜹니다.
4. 권한 화면에 **"Google Drive 파일…" 체크상자가 보이면 체크된 상태로** **계속**.
   - 범위가 하나뿐이면 체크상자가 없을 수도 있습니다.
   - 체크를 풀면 "드라이브 접근이 허용되지 않아 연결하지 못했습니다"가 뜹니다.
5. 첫 화면에 `☁ 내주소@gmail.com · 📂 드라이브 열기↗ · 새로고침 · 연결 해제 · 공용 PC: … Google 로그아웃↗`이 보이면 완료.
6. 로그인은 약 1시간 유지됩니다. 강의가 길면 1시간 뒤 첫 📌에서 로그인 창이 잠깐 뜰 수 있습니다.
   - **전체보기 중에는 로그인 창을 띄우지 않고** 위치를 보관합니다. 전체보기를 마친 뒤 📌를 한 번 더 누르세요.

---

## 5. 드라이브에 저장되는 것
- 내 드라이브의 **`Lecture Annotation Tool`** 폴더
  - ☁로 올린 PDF들
  - `library.json` — **구글 드라이브 저장** 목록(강의 이름·페이지)
- 연결된 상태에서 첫 화면의 **📂 드라이브 열기↗**를 누르면 이 폴더가 **새 탭**에서 열립니다(연결한 계정의 드라이브로 열림).
- **폴더 이름을 바꾸지 마세요**(이름으로 찾기 때문에 바꾸면 빈 목록이 됩니다. 폴더를 다른 위치로 옮기는 것은 괜찮습니다).
- **`library.json`은 옮기거나 이름을 바꾸지 마세요.**
- 드라이브 웹에서 이 폴더에 **직접 넣은 PDF는 앱에 보이지 않습니다**(앱이 만든 파일만 볼 수 있는 권한). 반드시 앱의 ☁로 올리세요.
- 목록에서 ✕로 지워도 **PDF 파일은 드라이브에 남습니다.** 필요 없으면 드라이브에서 직접 삭제하세요.

---

## 6. 자주 생기는 문제
| 증상 | 해결 |
|---|---|
| 연결 버튼을 눌러도 창이 안 뜸 | 브라우저 팝업 차단 해제 후 다시 클릭 |
| "Google 로그인 모듈을 불러오지 못했습니다" | 인터넷 연결 확인 → 광고 차단 확장 끄기 → 페이지 새로고침(F5). 학교망이 `accounts.google.com`을 막았을 수 있습니다 |
| "관리자가 이 앱을 차단했습니다" | 학교·교육청 Workspace 계정입니다. **개인 Gmail**로 로그인하세요 |
| "403: access_denied" | 3-3 **테스트 사용자**에 그 Gmail이 있는지 확인(또는 3-5 앱 게시) |
| "400: origin_mismatch" / "redirect_uri_mismatch" | 3-4 **승인된 JavaScript 원본** 철자·`https`·끝의 `/` 확인. 수정 후 몇 분 기다리기 |
| "드라이브 API가 허용되지 않았습니다" | 3-2 **Drive API 사용** 버튼을 눌렀는지 확인 |
| "드라이브 저장 공간이 부족합니다" | drive.google.com에서 저장 용량 확인(Gmail·사진도 같은 15GB를 씁니다). 폴더의 옛 PDF를 정리하세요 |
| "네트워크 연결을 확인해 주세요" / 목록에 "드라이브 목록을 불러오지 못했습니다" | 인터넷 확인 후 **새로고침** 버튼. 학교망이 드라이브를 막는 경우가 있으니 **리허설 + USB 백업** |
| "드라이브 연결이 만료되었습니다" | 로그인은 약 1시간 유지됩니다. **📌를 다시 누르면** 다시 로그인합니다(첫 화면이라면 연결 버튼) |
| "이 PC에는 저장했습니다. 드라이브에는 반영되지 않았습니다" | 로그인 창을 닫았거나 로그인이 만료된 경우입니다. 위치는 이 PC에 저장됐습니다. **📌를 다시 누르면** 드라이브에도 저장됩니다 |
| "드라이브에 아직 반영되지 않은 위치가 있습니다" | 전체보기 중 로그인이 만료돼 드라이브 반영을 미뤄 둔 상태입니다. 📌를 누르면 **현재 페이지**로 드라이브에도 저장됩니다 |
| "이미 최신입니다 — 드라이브 파일과 같은 내용입니다" | ⬆ 업데이트를 눌렀지만 두 파일 내용이 같아 아무것도 올리지 않았습니다(정상) |
| "이 PC 파일이 드라이브 파일보다 오래되었습니다 … 그래도 바꿀까요?" | 옛 버전(USB 사본 등)으로 드라이브를 덮어쓰려는 상황입니다. 확실하지 않으면 **취소**하세요 |
| "파일 접근 권한이 거부되어 드라이브를 업데이트하지 못했습니다" | 브라우저가 이 PC 파일 읽기를 허용해야 비교·업로드가 됩니다. ⬆를 다시 누르고 **허용**을 고르세요 |
| "드라이브에서 파일을 찾을 수 없습니다" | 드라이브에서 PDF를 지웠거나 휴지통에 있습니다. 목록에서 자동 제거됩니다. 휴지통에서 복원한 뒤 개인 PC에서 ☁를 다시 누르세요 |
| 목록에 "library.json을 읽을 수 없습니다" | 드라이브의 `Lecture Annotation Tool/library.json`이 손상됐습니다. 이 파일을 **휴지통으로 옮긴 뒤 새로고침** 버튼을 누르면 빈 목록으로 새로 시작합니다(PDF는 그대로. 필요한 강의는 ☁/📌로 다시 저장) |
| 목록이 텅 빔(폴더를 다룬 뒤) | 폴더 이름을 바꿨는지 확인 → 원래 이름 `Lecture Annotation Tool`로 되돌리고 새로고침 |
| 학교에서 연 강의가 수정 전 버전 | 개인 PC에서 수정본을 열고 **☁ → 덮어쓰기**를 누르세요(2장) |
| "드라이브에 같은 이름의 다른 버전이 있습니다" | 이 PC 파일과 드라이브 파일의 내용이 다릅니다. 이 PC의 파일로 바꾸려면 **☁ → 덮어쓰기**, 둘 다 남기려면 **☁ → 다른 이름으로 저장**. 그대로 두면 📌는 이 PC에만 저장합니다 |
| "이 PC에는 저장했습니다. 드라이브의 같은 이름 파일과 내용이 달라 드라이브에는 저장하지 않았습니다" | 다른 PC에서 드라이브 파일을 바꿨거나 이 PC 파일이 달라진 경우입니다. 드라이브 페이지는 그대로 둡니다. **☁**에서 덮어쓰기 또는 다른 이름으로 저장을 고르세요 |
| "…드라이브에서 이 강의 파일을 찾을 수 없어 드라이브에는 저장하지 않았습니다" | 드라이브에서 PDF를 지웠습니다. **☁ 드라이브 저장**으로 다시 올리세요 |
| 목록에 `강의자료 (2).pdf` 같은 강의가 따로 있음 | ☁에서 **다른 이름으로 저장**한 결과입니다. 원래 강의와 별개로 진도가 저장됩니다. 필요 없으면 ✕로 목록에서 지우세요 |
| 미리보기 주소(…-git-…vercel.app)에서는 안 됨 | 정상입니다. 3-4에 등록한 주소에서만 동작합니다 |

---

## 7. 개인정보·보안에 대해 정확히
- 앱의 **연결 정보(토큰)와 이메일**은 브라우저 **메모리에만** 있고 PC에 저장되지 않습니다. 탭을 닫거나 연결 해제하면 사라집니다.
- 하지만 **Google 로그인 자체는 브라우저에 남습니다.** 공용 PC에서는 반드시 **Google 로그아웃**(연결 해제 옆 링크)을 하거나 **시크릿 창**을 쓰세요.
- **드라이브에서 연 강의**는 학교 PC의 목록 저장소에 기록되지 않습니다.
  - 학교 PC에서 USB 파일을 파일 선택 창으로 열면, 그 파일 이름은 그 PC의 **저장된 강의 (이 PC)** 목록에 남습니다(기존 동작). 시크릿 창을 닫으면 함께 사라집니다.
- 이 앱은 **자기가 만든 파일만** 볼 수 있습니다(`drive.file` 권한).
- 권한을 완전히 거두려면: https://myaccount.google.com/permissions → Lecture Annotation Tool → 액세스 권한 삭제.
